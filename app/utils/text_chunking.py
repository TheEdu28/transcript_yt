"""Utilidades futuras para dividir transcripciones con solapamiento."""


def chunk_transcript(text: str, chunk_size: int, overlap: int) -> list[str]:
    """Split transcript text while preserving enough context for RAG."""
    raise NotImplementedError

